# Publish @t0.labs/daxta

Package is built and packed as `t0.labs-daxta-1.0.0.tgz`.

```bash
npm login
# create org t0.labs on npm if needed, then:
npm publish --access public
```

Until published, consumers can use:

```json
"@t0.labs/daxta": "file:../daxta"
```
